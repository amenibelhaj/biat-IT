# Base URL of the deployed backend API, including /api
# If unset, the app falls back to http://localhost:5000/api for local development.
REACT_APP_API_URL=https://your-backend-service.onrender.com/api
